(function() {
    'use strict';
    // var section = document.getElementById('dm');
    // const
    //     auth = firebase.auth();
    // auth.onAuthStateChanged(function (user) {
    //     if (user) {
    //         section.querySelector('.uid').innerText = firebase.auth().currentUser.uid;
    //     } else {
    //         section.querySelector('.uid').parentNode.innerText = "You’re not logged in."
    //     }
    // });
    return;
}());
